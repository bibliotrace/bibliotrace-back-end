describe("Audit service testing suite", () => {
  beforeAll(() => {});
  afterAll(() => {});
  test("Audit service tests not implemented yet, mostly because the audit service doesn't really do anything right now", () => {
    expect(1).toEqual(1);
  });
});
